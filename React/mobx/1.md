# 核心思想
状态变化引起的副作用应该被自动触发
- 应用逻辑只需哟啊修改状态数据即可，mobx会自动渲染UI，无需人工干预
- 数据比那户只会渲染对应的组件
- Mobx提供极致来存储盒更新应用状态供React使用
- react通过提供机制把应用状态转换为可渲染组件树并对其进行渲染

# api
autorun与reaction的区别   
autorun：一上来就执行，reaction监听到变化后才知行